#!/bin/bash

# This assumes that the machine running this script has already had the
# development environment playbook ran against it.
asdf install python 3.8.5
asdf global python 3.8.5
pip install --user ansible
pip install --user argcomplete
pip install --user psutil

